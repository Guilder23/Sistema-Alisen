document.addEventListener('DOMContentLoaded', function () {
    const table = document.getElementById('tablaProformas');
    if (!table) {
        return;
    }

    // Optional enhancements can be added here later.
    // For now, the proforma listing page only requires navigation links.
});
